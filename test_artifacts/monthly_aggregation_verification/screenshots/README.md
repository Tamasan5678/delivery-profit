# スクリーンショットについて

本番Emulator DBへ監査データを投入しない方針を優先したため、今回の35件・365件データを使った実機スクリーンショットは取得していない。

Homeの表示は1080×2424のWidgetテスト面で、月・週の値、単位、区切り、非同期切替、RenderFlex/ErrorWidget不在を自動検証した。既存端末DB、履歴、SharedPreferencesへの変更はない。
